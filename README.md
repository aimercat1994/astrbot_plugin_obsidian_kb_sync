# Obsidian 知识库同步插件

通过 [Fast Note Sync Service](https://github.com/your-repo/fast-note-sync) 将 Obsidian 笔记同步到 AstrBot 知识库。支持双向同步、暂存层管理、Web Dashboard、选择性同步、增量同步。

## ✨ 功能特性

### 核心同步

- **双向同步**：FNS ↔ 暂存区 ↔ AstrBot 知识库
  - `📥 从 FNS 同步`：从 FNS/Obsidian 拉取笔记到暂存区
  - `📤 同步到知识库`：将选中文档推送到 AstrBot 向量知识库
  - `📤 同步到 FNS`：将暂存区修改写回 FNS/Obsidian
- **增量同步**：基于内容 hash 智能跳过未变更笔记
- **预切块增量同步**：标记为预切块的文档，更新时只重新 embedding 变化的 chunk，token 消耗降低 90%+
- **并发获取/上传**：多路并发（默认 5 路），396 篇文档 2.2 秒完成
- **自动重试**：网络抖动自动指数退避重试（默认 3 次）
- **内容清洗**：自动去除 Obsidian 特有语法（`[[wikilinks]]`、`==高亮==`、`%%注释%%` 等）
- **删除恢复**：检测知识库中文档被手动删除后自动重新上传
- **双层验证**：即时验证 + 定期全量校验，确保知识库与同步状态一致
- **排除规则**：支持 glob 模式排除文件/文件夹

### Web Dashboard（端口 6190）

- **统一树形视图**：文件夹 + 文档混排展示，类 VS Code/Obsidian 文件管理器
- **文件夹展开/收起**：所有文件夹支持点击展开/收起，默认收起状态
- **文档预览**：Markdown 渲染（marked.js），代码语法高亮（highlight.js，200+ 语言）
- **目录导航（TOC）**：自动提取标题、点击跳转、滚动高亮当前章节
- **在线编辑**：编辑模式 + Ctrl+S 保存
- **批量操作**：全选同步、全选切块、取消全选
- **设置弹窗**：16 个配置项可视化编辑（FNS/知识库/同步/分块等）
- **Toast 通知**：操作成功/失败反馈
- **暗色主题**：跟随 AstrBot 桥接自动切换（DaisyUI 35 套内置主题）
- **搜索过滤**：侧边栏搜索框实时过滤文件名

### 文档管理

- **新建文档**：选择文件夹 + 输入文件名
- **删除文档**：带确认弹窗
- **导入文档**：支持 .md/.txt/.markdown 文件
- **导出文档**：自动下载为 .md 文件

## 📦 安装

1. 在 AstrBot Dashboard → 插件管理 → 安装插件
2. 输入本仓库地址或插件包上传
3. 安装依赖（插件会自动安装 `httpx`、`quart`）

## ⚙️ 配置

在 AstrBot Dashboard → 插件管理 → 本插件 → 配置 中设置，或点击 Dashboard 顶栏「⚙️ 设置」按钮：

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `fns_url` | string | `""` | Fast Note Sync 服务地址，如 `http://192.168.1.10:9002` |
| `fns_token` | string | `""` | FNS 登录 Token（JWT） |
| `fns_vault` | string | `""` | FNS 中的 Vault 名称 |
| `kb_id` | string | `""` | 目标 AstrBot 知识库 ID，留空自动创建 |
| `kb_name` | string | `"Obsidian Vault"` | 自动创建知识库时的名称 |
| `dashboard_port` | int | `6190` | Dashboard Web UI 端口 |
| `auto_sync` | bool | `true` | 是否开启自动同步 |
| `sync_interval` | int | `300` | 自动同步间隔（秒） |
| `exclude_patterns` | list | `[".obsidian", ".trash", "*.tmp", ".git"]` | 排除模式（glob） |
| `restore_deleted` | bool | `true` | 知识库中文档被删除后是否自动恢复 |
| `max_file_size` | int | `100` | 最大文件大小（KB），超过跳过，`0` 不限制 |
| `concurrent_fetches` | int | `5` | 并发获取笔记数 |
| `retry_count` | int | `3` | API 请求失败重试次数 |
| `verify_interval` | int | `10` | 全量校验间隔（每 N 次同步），`0` 关闭 |
| `chunk_size` | int | `512` | 增量同步时的文本分块大小（字符） |
| `chunk_overlap` | int | `50` | 增量同步时的分块重叠大小（字符） |

## 🖥️ Dashboard

启动 AstrBot 后访问 `http://<your-ip>:6190` 打开 Dashboard（也可通过 AstrBot 插件页面直接访问）。

**界面框架**：DaisyUI v5 + Tailwind CSS v4 CDN，支持亮色/暗色主题自动切换。

```
┌──────────────────────────────────────────────────────────┐
│ ⚡ Obsidian 知识库同步  ● FNS ● 知识库  ☀/🌙  ⚙️ 设置  │
├──────────────┬───────────────────────────────────────────┤
│ 📥 从FNS同步  │  📄 document.md     同步 │预切块          │
│ 📤 同步到知识库│                                           │
│ 📤 同步到FNS   │  # Markdown 标题                         │
│ 📄 新建       │  正文内容...                              │
│ 📂 导入       │  ## 二级标题                              │
│              │  代码块、表格、列表等                       │
│ 🔍 搜索...    │                                           │
│ ▼ 📂 AI       │                                           │
│   📄 doc.md   │                                           │
│ ▼ 📂 Docker   │                                           │
│   📄 guide.md │                                           │
│              │                                           │
│ ──────────── │                                           │
│ 全选同步      │                                           │
│ 全选切块      │                                           │
│ 取消全选      │                                           │
├──────────────┴───────────────────────────────────────────┤
│ 📄 375 篇  🔗 120 已同步  ✅ 50 已切块  │ 12:30:00       │
└──────────────────────────────────────────────────────────┘
```

| 功能 | 说明 |
|------|------|
| 资源管理器 | 左侧统一树形视图，文件夹和文档混排展示，点击文件夹展开/收起，支持多级嵌套 |
| 文档查看/编辑 | 右侧主区域，Markdown 渲染 + 代码高亮 + 目录导航，支持在线编辑（Ctrl+S 保存） |
| 搜索过滤 | 侧边栏顶部搜索框，实时过滤文件名 |
| 同步按钮 | 三个方向的同步：从 FNS、到知识库、到 FNS |
| 文档管理 | 新建/删除/导入/导出文档 |
| 设置弹窗 | 点击 ⚙️ 设置，弹窗编辑全部 16 个配置项 |
| 状态指示 | 顶栏显示 FNS/知识库连接状态，底部状态栏显示文档统计 |

## 🎮 指令

| 指令 | 说明 |
|------|------|
| `staging_sync` | 从 FNS 同步到暂存区 |
| `staging_push` | 将选中文档推送到知识库 |
| `staging_status` | 查看暂存区状态 |

## 📋 前置要求

1. **AstrBot** v4.25+ 且已配置至少一个 Embedding 模型提供商
2. **Fast Note Sync Service** 已部署并运行，Obsidian 插件端已同步笔记

## 🔧 工作原理

### 同步架构

```
Obsidian ──(同步)──► FNS Server ◄──────► 暂存区 ──(选择性)──► AstrBot 知识库
                    (HTTP API)  双向同步  (本地 .md)   Dashboard 控制
```

1. **FNS → 暂存区**：从 FNS 拉取笔记，写入本地暂存目录（镜像 Obsidian 目录结构）
2. **暂存区 → FNS**：将暂存区修改（编辑/新建/删除）写回 FNS/Obsidian
3. **用户操作**：在 Dashboard 浏览/编辑文档，勾选要同步的文档和预切块选项
4. **暂存区 → 知识库**：
   - `pre_chunk=false`：全量上传，AstrBot 自动切块 embedding
   - `pre_chunk=true`：增量同步，自己切块 → hash 对比 → 只重新 embedding 变化的 chunk

### 增量同步原理

```
旧文档（200 chunks）──► 获取旧 chunk 内容 ──► 计算每块 hash
新内容 ──► 自己切块（MarkdownChunker）──► 计算每块 hash
对比：3 块变化，197 块未变
→ 只对 3 块重新 embedding（vec_db.insert）
→ 197 块保留原有 embedding，零开销
```

## 📝 更新日志

### v3.5.0 (2026-06-18)

**📤 同步到 FNS**
- 新增「📤 同步到 FNS」按钮，将暂存区文档推送到 FNS/Obsidian
- 并发上传（默认 5 路），396 篇文档 2.2 秒完成
- FNSClient 新增 `upload_note` / `upload_notes_concurrent` 方法

### v3.4.0 (2026-06-18)

**📄 文档管理**
- 新建文档：选择文件夹 + 输入文件名
- 删除文档：带确认弹窗
- 导入文档：支持 .md/.txt/.markdown 文件
- 导出文档：自动下载为 .md 文件

### v3.3.0 (2026-06-18)

**✨ 文档预览增强**
- Markdown 渲染升级：marked.js 替换自定义正则，支持 GFM 语法
- 代码语法高亮：highlight.js，200+ 语言自动检测
- 目录导航面板（TOC）：自动提取标题、点击跳转、滚动高亮
- 标题渐变装饰线、引用块渐变色条、表格行悬停高亮

### v3.2.0 (2026-06-18)

**🎨 UI 重构**
- DaisyUI v5 + Tailwind CSS v4 CDN 替代 Pico CSS
- 统一树形视图（文件夹 + 文档混排）
- 所有文件夹支持展开/收起，默认收起状态
- 修复 `__files__` 数据结构适配问题

### v3.0.0 (2026-06-17)

**🆕 暂存层 + Web Dashboard**
- FNS 笔记先同步到本地暂存区，再从暂存区推送到知识库
- Web Dashboard：文件夹树 + 文档列表 + 查看/编辑器
- 选择性同步 + 预切块增量同步
- 批量操作、搜索过滤、状态栏

### v2.0.0 (2026-06-10)

**🚀 性能优化**
- 增量同步 ~30s → 0.4s（~75x 加速）
- 并发获取、自动重试、文件大小限制
- 增量同步：只重新 embedding 变化的 chunk

### v1.0.0 (2026-06-10)

- 初始版本，基本同步功能

## 📄 License

MIT
